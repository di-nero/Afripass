spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/afripass
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: 12345678901234567890123456789012
  expiration: 86400000

stripe:
  secret-key: sk_test_dummy_keyest